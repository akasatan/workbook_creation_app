<%= form_with model: @workbook , local: true do |f| %>
  <%= f.label :name, "問題集の名前" %>
  <%= f.text_field :name %>
  <%= f.select :disclosure_range, Workbook.disclosure_ranges.keys %>
  <%= f.submit "作成する！" %>
<% end %>