<% if obj.errors.any? %>
  <div id="errorexplanation">
    <h3><%= pluralize(obj.errors.count, "error") %>が発生しました。</h3>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>